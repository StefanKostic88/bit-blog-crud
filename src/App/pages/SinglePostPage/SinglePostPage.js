import { ContentWraper } from "../../components";

import { SinglePost } from "../../components";
const SinglePostPage = () => {
  return (
    <ContentWraper pageTitle="Single Post title">
      <SinglePost />
    </ContentWraper>
  );
};

export default SinglePostPage;
