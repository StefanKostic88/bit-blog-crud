import { ContentWraper, CreateNewPost } from "../../components";

const NewPost = () => {
  return (
    <ContentWraper pageTitle="New Post">
      <CreateNewPost />
    </ContentWraper>
  );
};

export default NewPost;
